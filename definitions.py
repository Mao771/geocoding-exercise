import os

ROOT_PATH = os.path.dirname(__file__)
CREDENTIALS = os.path.join(ROOT_PATH, 'creds.conf')
API_CACHE = os.path.join(ROOT_PATH, 'api_cache')
CSV_DIRECTORY = os.path.join(ROOT_PATH, 'sample_csv')
SAMPLE_CSV_RESULT = os.path.join(CSV_DIRECTORY, 'sample_result.csv')
